{{ form_start(form) }}
<div class="form-group">
    {{ form_row(form.type) }}
</div>
<div class="form-group"></div>
<div class="form-group">
    {{ form_label(form.demiJourner) }}
    {{ form_widget(form.demiJourner) }}
</div>
<div class="form-group">
    {{ form_row(form.debut) }}</div>
<div class="form-group">
    {{ form_row(form.fin) }}</div>
<div class="col-12">
    <a href="#" class="btn btn-secondary">Cancel</a>
    <input type="submit" value="{{ button_label|default('Save') }}" class="btn btn-success float-right"></div>
    {{ form_end(form) }}
