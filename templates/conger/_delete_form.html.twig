<form method="post" action="{{ path('conger_delete', {'id': conger.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ conger.id) }}">
    <button class="btn">Delete</button>
</form>
